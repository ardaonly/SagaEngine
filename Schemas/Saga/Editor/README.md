# saga.editor Schema Package

This directory owns Saga-specific editor composition schema source.

SDE remains a generic compiler. Saga editor composition adapters and tools may
compile this package through SDE public facade and graph APIs, but this schema
package is not part of the standalone SDE package tree.
