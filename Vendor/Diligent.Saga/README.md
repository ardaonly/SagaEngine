# Saga Diligent Integration Metadata

This directory is owned by SagaEngine, not by the Diligent submodule.

`Vendor/Diligent` is expected to be a git submodule that points at the
Saga-owned DiligentCore fork. Keep Saga-specific integration notes here so the
main repository tracks them independently of the vendor fork.

