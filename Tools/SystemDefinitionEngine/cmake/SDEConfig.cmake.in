@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
find_dependency(nlohmann_json CONFIG)

include("${CMAKE_CURRENT_LIST_DIR}/SDETargets.cmake")

if(TARGET SDE::Core AND NOT TARGET SDE::SDE)
    add_library(SDE::SDE ALIAS SDE::Core)
endif()

check_required_components(SDE)
