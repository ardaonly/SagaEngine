#pragma once
#include <string>
namespace SagaEditor {
/// Identifies a named extension hook (e.g. "saga.toolbar.main", "saga.menu.asset")
using ExtensionPoint = std::string;
} // namespace SagaEditor
