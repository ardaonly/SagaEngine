name: SagaEngine CI

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: sagaforge
        shell: pwsh

    steps:
      - name: Checkout
        uses: actions/checkout@v4