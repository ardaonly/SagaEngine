/// @file RenderCommand.cpp
/// @brief CommandRecorder implementation — currently header-only, this file
///        anchors future non-inline helpers (batch recording, command stats).

#include "SagaEngine/Render/CommandRecording/CommandRecorder.h"

namespace SagaEngine::Render::CommandRecording
{

// All current methods are inline in the header.
// Future additions:
//   void CommandRecorder::RecordDrawItems(const RenderView& view);
//   CommandStats CommandRecorder::GetStats() const;

} // namespace SagaEngine::Render::CommandRecording
