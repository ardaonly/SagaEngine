# Phase 17 Known Limitations

- This phase is not verified.
- The proof is CLI-only and test-level orchestration.
- No new SagaScript CLI command is added.
- No SagaScript source changes are included in this phase.
- Only one `StringLiteralEdit` path is proven.
- Original fixture files are not mutated.
- Patched output is written only to temporary copied output.
- No in-place editing exists.
- No editor UI or editor workflow exists.
- No full Visual Blocks editor exists.
- No runtime graph execution exists.
- No arbitrary C# to blocks conversion is claimed.
- Runtime, server, package/distribution, SDE, StarterArena gameplay,
  SagaRuntime smoke path, and CSharpScriptHost behavior are unchanged.
