# Phase 22 Known Limitations

- This phase is not verified.
- No public product claim should be derived from this file.
- Phase 22 implements a no-UI editor shell inspection/report path only.
- No full editor dashboard workflow is implemented by this phase.
- Workflow actions are command/report references and are not executed by
  `SagaEditor --inspect-project`.
- Visual Blocks evidence remains CLI-only and is not Visual Blocks editor UI.
- No drag/drop editing or in-place C# editing is implemented.
- `scripts/package-linux-saga` remains preflight-only and is not package or
  distribution readiness.
- Failed tools, missing reports, and package preflight failures must remain
  visible in any future UI.
- Phase 22 does not modify Runtime, Server, SagaScript, SDE,
  package/distribution scripts, StarterArena gameplay, SagaRuntime smoke paths,
  CSharpScriptHost, Visual Blocks editor UI, or collaboration server behavior.
