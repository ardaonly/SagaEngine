# Phase 34 Commands Log

## 2026-06-04

```bash
nix-shell --run "python3 Tools/SystemDefinitionEngine/build.py --clean --jobs 1"
```

Result: passed. The command built the real `sde-cli` target and staged the real
SDE CLI at `Tools/SystemDefinitionEngine/bin/sde`.

```bash
test -x Tools/SystemDefinitionEngine/bin/sde
```

Result: passed.

```bash
scripts/package-linux-saga
```

Result: passed. The script staged `build/dist/linux/Saga`, generated
`build/dist/linux/Saga.tar.zst`, generated `build/dist/linux/Saga.sha256`, and
exited `0`. The report kept `verified: false` and `missingInputs: []`.

```bash
python3 -m json.tool build/reports/linux_package_preflight_report.json
```

Result: passed. The report recorded `status: "preflight-passed"`,
`archiveStatus.status: "available"`, `checksumStatus.status: "available"`, and
readiness non-claims.

```bash
test -d build/dist/linux/Saga
test -f build/dist/linux/Saga.tar.zst
test -f build/dist/linux/Saga.sha256
```

Result: passed.

```bash
cd build/dist/linux && sha256sum -c Saga.sha256
```

Result: passed. Output:

```txt
Saga.tar.zst: OK
```

```bash
tar -tf build/dist/linux/Saga.tar.zst
```

Result: passed. The archive lists `Saga/` as the top-level directory and contains
staged-layout paths only.

```bash
python3 - <<'PY'
import json
from pathlib import Path

report = Path("build/reports/linux_package_preflight_report.json")
data = json.loads(report.read_text())

assert data.get("verified") is False

archive = Path("build/dist/linux/Saga.tar.zst")
checksum = Path("build/dist/linux/Saga.sha256")
layout = Path("build/dist/linux/Saga")

assert layout.exists(), "staged layout missing"
assert archive.exists(), "archive missing"
assert checksum.exists(), "checksum missing"

missing_blob = json.dumps(data.get("missingInputs", [])) + json.dumps(data.get("diagnostics", []))
for needle in ["Saga.tar.zst", "Saga.sha256"]:
    assert needle not in missing_blob, f"archive/checksum blocker still present: {needle}"

non_claims = json.dumps(data.get("nonClaims", []))
for needle in ["production", "enterprise", "verified release"]:
    assert needle.lower() in non_claims.lower(), f"missing non-claim: {needle}"

print("Phase 34 archive/checksum evidence is valid; readiness remains non-claimed.")
PY
```

Result: passed.

```bash
git diff --check
```

Result: passed.

```bash
scripts/scan-claims README.md docs samples Tools
```

Result: passed. Scanned 188 public text files; no unsupported public claim text
found.

```bash
scripts/verify-quick
```

Result: passed. This did not mark any phase Verified.

```bash
scripts/verify-local --allow-dirty
```

Result: passed. Dirty worktree was allowed for pre-commit verification; this did
not mark any phase Verified.

```bash
scripts/verify-phase 34
```

Result: passed. Phase 34 structure checks passed; no phase was marked Verified.
