# Phase 31 Commands Log

## 2026-06-04

```bash
git diff --check
```

Result: passed.

```bash
scripts/scan-claims README.md docs samples Tools
```

Result: passed. Scanned 186 public text files; no unsupported public claim text
found.

```bash
scripts/verify-quick
```

Result: passed. This did not mark any phase Verified.

```bash
scripts/verify-local --allow-dirty
```

Result: passed. Real build was false; all-safe tests were false; dirty worktree
was allowed for pre-commit verification.

```bash
scripts/verify-phase 31
```

Result: passed. Phase 31 structure checks passed; no phase was marked Verified.

```bash
scripts/package-linux-saga
```

Result: expected blocked preflight, exit code `1`. The command wrote
`build/reports/linux_package_preflight_report.json` with `status: "blocked"`
and `verified: false`.

Current blockers:

- `sde CLI: missing real executable; checked build/RelWithDebInfo-0.0.9-sde/bin/sde, Tools/SystemDefinitionEngine/build/bin/sde, Tools/SystemDefinitionEngine/build/sde`
- `expected final distribution layout: missing directory build/dist/linux/Saga`
- `expected distribution VERSION: missing file build/dist/linux/Saga/VERSION`
- `expected distribution VERIFY.txt: missing file build/dist/linux/Saga/VERIFY.txt`
- `expected distribution known limitations: missing file build/dist/linux/Saga/KNOWN_LIMITATIONS.md`
- `expected distribution archive: missing file build/dist/linux/Saga.tar.zst`
- `expected distribution checksum: missing file build/dist/linux/Saga.sha256`

```bash
python3 -m json.tool build/reports/linux_package_preflight_report.json
```

Result: passed. The default report parsed as JSON and recorded
`withoutServer: false`.

```bash
scripts/package-linux-saga --without-server --report-out /tmp/linux_package_preflight_without_server_report.json
```

Result: expected blocked preflight, exit code `1`. The report parsed as JSON and
recorded `withoutServer: true`; the remaining blockers were unchanged.

```bash
scripts/package-linux-saga --report-out /tmp/linux_package_preflight_report.json
```

Result: expected blocked preflight, exit code `1`. The redirected report parsed
as JSON and recorded `withoutServer: false`.
