# Phase 35 Commands Log

## 2026-06-04

```bash
nix-shell --run "python3 Tools/SystemDefinitionEngine/build.py --clean --jobs 1"
```

Result: passed. The command built the real `sde-cli` target and staged the real
SDE CLI at `Tools/SystemDefinitionEngine/bin/sde`.

```bash
test -x Tools/SystemDefinitionEngine/bin/sde
```

Result: passed.

```bash
scripts/package-linux-saga
```

Result: passed. The command regenerated the staged layout, `Saga.tar.zst`, and
`Saga.sha256` without readiness claims.

```bash
python3 -m json.tool build/reports/linux_package_preflight_report.json
```

Result: passed.

```bash
cd build/dist/linux && sha256sum -c Saga.sha256
```

Result: passed. Output:

```txt
Saga.tar.zst: OK
```

```bash
tar -tf build/dist/linux/Saga.tar.zst >/tmp/saga_archive_listing.txt
```

Result: passed. The archive listing was written to
`/tmp/saga_archive_listing.txt` and confirmed `Saga/` as the archive root.

```bash
scripts/smoke-linux-saga-dist \
  --archive build/dist/linux/Saga.tar.zst \
  --checksum build/dist/linux/Saga.sha256 \
  --work-dir /tmp/saga_dist_smoke \
  --report-out build/reports/linux_distribution_smoke_report.json
```

Result: passed. The script returned `passed-with-limitations`, unpacked the
archive into `/tmp/saga_dist_smoke/Saga`, and wrote
`build/reports/linux_distribution_smoke_report.json`.

Required unpacked checks passed for:

- `Saga/bin/Saga`
- `Saga/bin/SagaEditor`
- `Saga/bin/SagaRuntime`
- `Saga/bin/SagaServer`
- `Saga/tools/sde`
- `Saga/tools/sagaproject`
- `Saga/tools/sagascript`
- `Saga/tools/sagapack`
- `Saga/samples/StarterArena`
- `Saga/BUILD_INFO.json`
- `Saga/VERSION`
- `Saga/VERIFY.txt`
- `Saga/KNOWN_LIMITATIONS.md`

Required help commands passed for:

- `Saga/tools/sde --help`
- `Saga/bin/Saga --help`
- `Saga/bin/SagaRuntime --help`
- `Saga/bin/SagaServer --help`

Recorded limitations:

- `Saga/bin/SagaEditor --help` is unsupported.
- `Saga/tools/sagaproject --help`, `Saga/tools/sagascript --help`, and
  `Saga/tools/sagapack --help` fail from the unpacked distribution because the
  wrappers expect adjacent `.csproj` files that are not packaged.

```bash
python3 -m json.tool build/reports/linux_distribution_smoke_report.json
```

Result: passed. The report recorded `status: "passed-with-limitations"` and
`verified: false`.

```bash
python3 - <<'PY'
import json
from pathlib import Path

p = Path("build/reports/linux_distribution_smoke_report.json")
data = json.loads(p.read_text())

assert data.get("verified") is False
assert data.get("status") in {"passed-with-limitations", "passed", "blocked"}

checks = json.dumps(data.get("checks", []))
for needle in ["Saga/bin/Saga", "Saga/tools/sde", "Saga/samples/StarterArena"]:
    assert needle in checks, f"missing smoke check for {needle}"

non_claims = json.dumps(data.get("nonClaims", []))
for needle in ["production", "enterprise", "verified final release"]:
    assert needle.lower() in non_claims.lower(), f"missing non-claim: {needle}"

print("Phase 35 distribution unpack smoke report is valid and non-overclaiming.")
PY
```

Result: passed. Output:

```txt
Phase 35 distribution unpack smoke report is valid and non-overclaiming.
```

```bash
git diff --check
```

Result: passed.

```bash
scripts/scan-claims README.md docs samples Tools
```

Result: passed. Scanned 189 public text files; no unsupported public claim text
found.

```bash
scripts/verify-quick
```

Result: passed. This did not mark any phase Verified.

```bash
scripts/verify-local --allow-dirty
```

Result: passed. Dirty worktree was allowed for pre-commit verification; this did
not mark any phase Verified.

```bash
scripts/verify-phase 35
```

Result: passed. Phase 35 structure checks passed; no phase was marked Verified.
